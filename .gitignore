# Agent scratch (indexes, plans, receipts). Promote durable knowledge into docs/ or source.
.agents/
.agent/

# Rust
**/target/
**/*.rs.bk

# Godot
**/.godot/
# export_presets.cfg is committed (desktop Win/Mac/Linux presets). Credentials stay out.
**/.godot/export_credentials.cfg
*.translation

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Secrets (never commit)
.env
*.key

# Desktop export output (presets write under builds/)
builds/**
!builds/README.md

# Generated art. Raw provider output is large and regenerable from the spec.
art/raw/
art/**/*_preview.png

# Subagent worktrees are checkouts, not content.
.claude/worktrees/
