[package]
name = "fragr-tip-gate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Developer-only check that historical jammer tip stills show the orange dish"
publish = false

[[bin]]
name = "gate-tip-jammer-orange"
path = "src/main.rs"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }

[lints]
workspace = true
